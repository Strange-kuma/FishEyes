{
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^8.35.0",
    "yorkie": "^2.0.0"
  },
  "scripts": {
    "lint": "eslint --ext .html,.js,.vue --max-warnings=0 ."
  },
  "gitHooks": {
    "pre-push": "npm run lint"
  }
}
